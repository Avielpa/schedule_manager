# Minimal Requirements - Only Essential Packages
# Generated based on actual code analysis

# Core Django Framework
Django>=4.2.0,<5.0
djangorestframework>=3.14.0

# Scheduling Algorithm Engine  
ortools>=9.7.0

# Excel Export Functionality
openpyxl>=3.0.0

# Background Task Processing
celery>=5.2.0

# API Enhancement (Used in Settings)
django-cors-headers>=4.0.0
django-filter>=23.0.0
drf-spectacular>=0.26.0

# Development/Debug (Remove in Production)
django-debug-toolbar>=4.0.0

# Installation:
# pip install -r requirements.txt

# Packages removed from original requirements:
# - pandas, numpy, scipy (not imported in code)
# - matplotlib, seaborn, plotly (visualization not used)  
# - xlsxwriter (using openpyxl instead)
# - python-dateutil, structlog (not directly used)
# - psycopg2-binary, redis (not using PostgreSQL/Redis)
# - django-ratelimit, cryptography (security packages not used)
# - pytest packages, black, flake8, mypy (development tools)
# - memory-profiler, psutil, sentry-sdk (monitoring not used)
# - django-extensions, django-security, django-csp (extras not used)
#
# Total: Reduced from ~25 packages to 9 essential packages
